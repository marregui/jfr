// Root project: no code, only the shared description. Each module configures itself
// explicitly (see core/, cli/, netty-demo/) so that a reader of one module sees its
// whole build without chasing convention plugins.

group = "dev.jfrq"
version = "0.1.0"
